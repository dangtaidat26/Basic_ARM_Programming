#include "MKL46Z4.h"                    // NXP::Device:Startup

void blink(void);

int main (void)
{
	/* This function is used to Init Green LED and make it blink forever */
	blink();
	
	while(1)
	{
	}
	return 0;
}
